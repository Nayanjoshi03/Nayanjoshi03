{% extends "layout.html" %}
{% block body %}
    {%for post in posts %}
    <div class="post">
        <h1>{{post.title}}</h1>
        <p>{{post.author}}</p>
        <p>{{post.content}}</p>
    </div>
    {%  endfor %}
{% endblock body   %} 